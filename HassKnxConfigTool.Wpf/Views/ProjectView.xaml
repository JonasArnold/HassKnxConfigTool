<UserControl x:Class="HassKnxConfigTool.Wpf.Views.ProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HassKnxConfigTool.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="10 0">
                    <Label Content="Projects"/>
                    <DataGrid ItemsSource="{Binding Projects}" AutoGenerateColumns="True"></DataGrid>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10 0">
                    <Label Content="New Project Name"/>
                    <TextBox Text="{Binding NewProjectName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 20"/>
                    <Button Command="{Binding AddProjectCommand}" IsEnabled="{Binding CanAddProject}" Margin="0 0 0 20" Content="Add New Project"/>
                </StackPanel>

            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
